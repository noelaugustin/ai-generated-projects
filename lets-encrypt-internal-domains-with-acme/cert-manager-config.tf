# This file is now empty as ClusterIssuers are managed via the ops-config helm chart.
# This ensures that the configuration is applied only after cert-manager is ready,
# avoiding Terraform provider initialization issues.
